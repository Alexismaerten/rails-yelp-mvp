<h1> New Reviews</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
<h3><%= link_to " back to index" , restaurants_path, class: "btn btn-primary"  %></h3>
